# LibSQL/Turso instance — HTTP or libsql://, never a .db file path
LIBSQL_URL=http://127.0.0.1:8080

# Turso Cloud (same apply-schema.ts / drizzle-kit push):
# TURSO_DATABASE_URL=libsql://upcoming-db-<org>.turso.io
# TURSO_AUTH_TOKEN=
# Daily.co (video rooms) - server only, do not commit real key
DAILY_API_KEY=
